# Copier en .env (non versionné) et adapter

PORT=3000

# Clé partagée avec les bornes. Doit être identique à CLE_BORNE dans borne/config.h
CLE_BORNE=changez-moi-avant-la-demo

# Fichier de base de données
DB_PATH=./data.db
